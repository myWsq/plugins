{
  "description": "Forward Claude Code hook events to the local coflux daemon through the cofluxd messenger (silent no-op when cofluxd is not installed); at session start, inside a coflux terminal, print the <coflux-session> block with the session's coflux coordinates; on every prompt, print a <coflux-session-moved> block when the working directory sits in a different coflux workspace than the terminal does; follow the agent into git worktrees (EnterWorktree, ExitWorktree, resume and Claude Code's own cleanup move the terminal's owning workspace, registering an unknown worktree first); inside a coflux project session, steer `git worktree remove|move` to the coflux MCP workspace tools.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c '[ -r \"$0\" ] && exec sh \"$0\" || :' \"${CLAUDE_PLUGIN_ROOT}/scripts/session-context.sh\"",
            "timeout": 8
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v node >/dev/null 2>&1 && exec node \"$0\" || :' \"${CLAUDE_PLUGIN_ROOT}/scripts/session-moved.mjs\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v node >/dev/null 2>&1 && exec node \"$0\" || :' \"${CLAUDE_PLUGIN_ROOT}/scripts/guard-git-worktree.mjs\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      },
      {
        "matcher": "EnterWorktree|ExitWorktree",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v node >/dev/null 2>&1 && exec node \"$0\" || :' \"${CLAUDE_PLUGIN_ROOT}/scripts/worktree-follow.mjs\"",
            "timeout": 15
          }
        ]
      }
    ],
    "WorktreeRemove": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v node >/dev/null 2>&1 && exec node \"$0\" || :' \"${CLAUDE_PLUGIN_ROOT}/scripts/worktree-follow.mjs\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      }
    ],
    "StopFailure": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'command -v cofluxd >/dev/null 2>&1 && exec cofluxd hook claude || :'"
          }
        ]
      }
    ]
  }
}
